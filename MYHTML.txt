<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<title>Bappa Aaye Hain ! A Blessing for You</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,500;0,9..144,600;1,9..144,500&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root{
    --cream:#F8EEDC;
    --cream-deep:#EFDFBE;
    --ink:#2B1810;
    --ink-soft:#5A4433;
    --maroon:#7A2131;
    --maroon-deep:#4E1420;
    --saffron:#DE7B34;
    --gold:#C79A3F;
    --gold-light:#E7CE8F;
    --night:#181109;
    --night-deep:#0F0A05;
    --ease:cubic-bezier(.16,.84,.44,1);
  }

  *{box-sizing:border-box;}
  html,body{margin:0;padding:0;}
  body{
    font-family:'Inter',sans-serif;
    color:var(--ink);
    background:var(--night);
    overflow:hidden;
    height:100dvh;
    width:100vw;
    -webkit-tap-highlight-color:transparent;
  }

  @media (prefers-reduced-motion: reduce){
    *{animation-duration:.001ms !important; animation-iteration-count:1 !important; transition-duration:.001ms !important;}
  }

  h1,h2,h3{font-family:'Fraunces',serif; margin:0; font-weight:500;}

  #stage{
    position:relative;
    height:100dvh;
    width:100vw;
    overflow:hidden;
  }

  .screen{
    position:absolute;
    inset:0;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:8vh 6vw;
    opacity:0;
    pointer-events:none;
    transition:opacity .9s var(--ease);
  }
  .screen.active{
    opacity:1;
    pointer-events:auto;
  }

  .screen-inner{
    position:relative;
    z-index:4;
    display:flex;
    flex-direction:column;
    align-items:center;
    text-align:center;
    gap:clamp(18px,3vh,34px);
    max-width:520px;
    width:100%;
  }

  /* ---------- Ambient backgrounds ---------- */
  .bg-mandala{
    position:absolute;
    inset:0;
    background:
      radial-gradient(circle at 50% 38%, rgba(199,154,63,.10), transparent 60%);
    pointer-events:none;
  }
  #screen1{ background: radial-gradient(circle at 50% 30%, #241708 0%, var(--night) 55%, var(--night-deep) 100%); }
  #screen2, #screen3, #screen4{
    background:
      radial-gradient(circle at 50% 0%, rgba(222,123,52,.16), transparent 55%),
      linear-gradient(180deg, var(--cream) 0%, var(--cream-deep) 100%);
  }

  .arch-outline{
    position:absolute;
    top:6%;
    left:50%;
    transform:translateX(-50%);
    width:min(340px, 70vw);
    height:auto;
    opacity:.16;
    pointer-events:none;
  }
  #screen1 .arch-outline{ opacity:.10; }

  /* ---------- Particles (gold dust) ---------- */
  .particle-field{ position:absolute; inset:0; pointer-events:none; overflow:hidden; }
  .dust{
    position:absolute;
    border-radius:50%;
    background:radial-gradient(circle, var(--gold-light), transparent 70%);
    opacity:0;
    animation:twinkle 3.6s ease-in-out infinite;
  }
  @keyframes twinkle{
    0%,100%{ opacity:0; transform:scale(.6) translateY(0);}
    50%{ opacity:.85; transform:scale(1) translateY(-6px);}
  }

  /* ---------- Petals ---------- */
  .petal{
    position:absolute;
    top:-5%;
    width:10px;
    height:10px;
    background:linear-gradient(135deg, var(--saffron), var(--maroon));
    border-radius:0 60% 0 60%;
    opacity:.85;
    animation:fall linear infinite;
  }
  @keyframes fall{
    0%{ transform:translateY(0) rotate(0deg); }
    100%{ transform:translateY(110vh) rotate(340deg); }
  }

  /* ---------- Screen 1 : Arrival ---------- */
  #screen1 .screen-inner{ color:var(--cream); }
  .glyph{
    width:clamp(140px,26vw,190px);
    height:auto;
    opacity:.92;
    filter:drop-shadow(0 0 28px rgba(199,154,63,.35));
    animation:hover-idle 6s ease-in-out infinite;
  }
  @keyframes hover-idle{
    0%,100%{ transform:translateY(0px); }
    50%{ transform:translateY(-10px); }
  }
  .eyebrow-line{
    font-size:.9rem;
    letter-spacing:.02em;
    color:var(--gold-light);
    opacity:.85;
  }
  .headline-1{
    font-size:clamp(1.7rem,5.6vw,2.5rem);
    font-style:italic;
    color:var(--cream);
  }
  .sub-1{ color:#D8C6AE; font-size:.98rem; max-width:34ch; line-height:1.55; }

  /* ---------- Temple doorway ---------- */
  :root{ --door-w:190px; }
  @media (min-width:480px){ :root{ --door-w:216px; } }
  @media (min-width:900px){ :root{ --door-w:250px; } }
  @media (max-height:640px){ :root{ --door-w:150px; } }

  .doorway{
    position:relative;
    width:var(--door-w);
    height:calc(var(--door-w) * 1.32);
    margin:0 auto;
    border-radius:calc(var(--door-w) / 2) calc(var(--door-w) / 2) 8px 8px;
    overflow:hidden;
    perspective:900px;
    background:#0C0803;
    box-shadow:
      0 26px 60px -20px rgba(0,0,0,.7),
      0 0 0 6px rgba(199,154,63,.14);
  }
  .murti-spot{
    position:absolute;
    inset:0;
    display:flex;
    align-items:center;
    justify-content:center;
    background:radial-gradient(circle at 50% 42%, rgba(199,154,63,.28), transparent 68%);
    z-index:1;
  }
  .murti-spot .glyph{
    width:62%;
    opacity:0;
    transform:scale(.7);
    transition:opacity 1s var(--ease) .35s, transform 1s var(--ease) .35s;
  }
  .doorway.open .murti-spot .glyph{ opacity:1; transform:scale(1); }

  .door-panel{
    position:absolute;
    top:0;
    height:100%;
    width:50%;
    z-index:2;
    background:
      repeating-linear-gradient(90deg, rgba(0,0,0,.12) 0 2px, transparent 2px 14px),
      linear-gradient(160deg, #4A2C18, #1E1208 82%);
    transition:transform 1.15s cubic-bezier(.7,0,.24,1);
    box-shadow:inset 0 0 30px rgba(0,0,0,.5);
  }
  .door-panel::before{
    content:"";
    position:absolute;
    top:16px; bottom:16px; left:12px; right:12px;
    border:1px solid rgba(199,154,63,.3);
    border-radius:6px;
  }
  .door-panel.left{ left:0; transform-origin:left center; border-right:1px solid rgba(199,154,63,.4); }
  .door-panel.right{ right:0; transform-origin:right center; border-left:1px solid rgba(199,154,63,.4); }
  .door-panel.left::after,
  .door-panel.right::after{
    content:"";
    position:absolute;
    top:50%;
    width:7px; height:7px;
    border-radius:50%;
    background:var(--gold-light);
    box-shadow:0 0 9px rgba(231,206,143,.7);
    transform:translateY(-50%);
  }
  .door-panel.left::after{ right:9px; }
  .door-panel.right::after{ left:9px; }

  .doorway.open .door-panel.left{ transform:rotateY(-108deg); }
  .doorway.open .door-panel.right{ transform:rotateY(108deg); }

  .door-flash{
    position:absolute;
    inset:0;
    z-index:3;
    background:radial-gradient(circle, rgba(231,206,143,.9), transparent 70%);
    opacity:0;
    pointer-events:none;
  }
  .door-flash.burst{ animation:flashPulse .9s ease-out; }
  @keyframes flashPulse{
    0%{ opacity:0; transform:scale(.6); }
    30%{ opacity:.85; transform:scale(1.15); }
    100%{ opacity:0; transform:scale(1.4); }
  }

  .door-hint{
    font-size:.78rem;
    color:#B79A7A;
    letter-spacing:.01em;
  }

  /* ---------- Buttons ---------- */
  .btn{
    appearance:none;
    border:none;
    cursor:pointer;
    font-family:'Inter',sans-serif;
    font-size:1rem;
    font-weight:600;
    padding:15px 34px;
    border-radius:999px;
    letter-spacing:.01em;
    transition:transform .35s var(--ease), box-shadow .35s var(--ease), background .35s var(--ease);
  }
  .btn:active{ transform:scale(.96); }
  .btn-primary{
    background:linear-gradient(135deg, var(--saffron), var(--maroon));
    color:var(--cream);
    box-shadow:0 14px 30px -10px rgba(122,33,49,.55);
  }
  .btn-primary:hover{ box-shadow:0 18px 38px -10px rgba(122,33,49,.7); }
  .btn-ghost{
    background:transparent;
    color:var(--maroon);
    border:1.5px solid rgba(122,33,49,.35);
    padding:13px 28px;
  }
  .btn-ghost:hover{ background:rgba(122,33,49,.06); }
  #screen1 .btn-primary{ box-shadow:0 14px 34px -8px rgba(0,0,0,.6); }

  .btn-text{
    background:none; border:none; cursor:pointer;
    color:var(--maroon-deep); font-family:'Inter',sans-serif;
    font-size:.9rem; font-weight:500; text-decoration:underline;
    text-underline-offset:4px; text-decoration-color:rgba(122,33,49,.35);
    padding:6px;
  }

  /* ---------- Screen 2 : Transformation ---------- */
  .diya-row{
    display:flex;
    gap:clamp(10px,3vw,20px);
    align-items:flex-end;
    height:56px;
  }
  .diya{ width:26px; height:auto; opacity:.5; transform:translateY(4px); transition:opacity .5s, transform .5s, filter .5s; }
  .diya .flame{ transform-origin:50% 100%; opacity:0; }
  .diya-row.lit .diya{ opacity:1; transform:translateY(0); }
  .diya-row.lit .diya .flame{
    animation:flicker 1.6s ease-in-out infinite;
  }
  @keyframes flicker{
    0%,100%{ opacity:.95; transform:scaleY(1) scaleX(1);}
    50%{ opacity:.7; transform:scaleY(.85) scaleX(.92);}
  }
  .diya-row.lit .diya:nth-child(1) .flame{ animation-delay:.05s; }
  .diya-row.lit .diya:nth-child(2) .flame{ animation-delay:.3s; }
  .diya-row.lit .diya:nth-child(3) .flame{ animation-delay:.55s; }
  .diya-row.lit .diya:nth-child(4) .flame{ animation-delay:.8s; }
  .diya-row.lit .diya:nth-child(5) .flame{ animation-delay:1.05s; }

  .ganpati-reveal{
    width:clamp(150px,28vw,200px);
    opacity:0;
    transform:scale(.85);
    filter:blur(6px);
    transition:opacity 1.1s var(--ease), transform 1.1s var(--ease), filter 1.1s var(--ease);
  }
  #screen2.revealed .ganpati-reveal{ opacity:1; transform:scale(1); filter:blur(0); }

  .headline-2{
    font-size:clamp(1.9rem,6.4vw,2.9rem);
    color:var(--maroon-deep);
    opacity:0; transform:translateY(10px);
    transition:opacity .9s var(--ease) .3s, transform .9s var(--ease) .3s;
  }
  #screen2.revealed .headline-2{ opacity:1; transform:translateY(0); }

  .sub-2{
    color:var(--ink-soft); font-size:.98rem; max-width:32ch; line-height:1.55;
    opacity:0; transform:translateY(8px);
    transition:opacity .9s var(--ease) .55s, transform .9s var(--ease) .55s;
  }
  #screen2.revealed .sub-2{ opacity:1; transform:translateY(0); }

  .cta-2{
    opacity:0; transform:translateY(8px);
    transition:opacity .9s var(--ease) .85s, transform .9s var(--ease) .85s;
  }
  #screen2.revealed .cta-2{ opacity:1; transform:translateY(0); }

  /* ---------- Screen 3 : Blessing ---------- */
  .kicker{
    font-family:'Fraunces',serif;
    font-style:italic;
    color:var(--saffron);
    font-size:1.05rem;
  }
  .headline-3{ font-size:clamp(1.7rem,5.8vw,2.4rem); color:var(--maroon-deep); }

  .blessing-card{
    background:linear-gradient(180deg, #FFFCF5, var(--cream));
    border:1px solid rgba(199,154,63,.35);
    border-radius:22px;
    padding:clamp(24px,5vw,36px) clamp(20px,5vw,30px);
    box-shadow:0 24px 50px -22px rgba(78,20,32,.35);
    min-height:118px;
    display:flex;
    align-items:center;
    justify-content:center;
    width:100%;
  }
  .blessing-card p{
    font-family:'Fraunces',serif;
    font-size:clamp(1.15rem,3.6vw,1.4rem);
    line-height:1.5;
    color:var(--maroon-deep);
    margin:0;
    opacity:0;
    transform:translateY(6px);
    transition:opacity .6s var(--ease), transform .6s var(--ease);
  }
  .blessing-card p.show{ opacity:1; transform:translateY(0); }

  .row-actions{ display:flex; flex-direction:column; gap:14px; align-items:center; width:100%; }

  /* ---------- Screen 4 : Personalize ---------- */
  .field{
    width:100%;
    display:flex;
    flex-direction:column;
    gap:8px;
    text-align:left;
  }
  .field label{ font-size:.85rem; color:var(--ink-soft); font-weight:500; }
  .field input{
    font-family:'Fraunces',serif;
    font-size:1.15rem;
    padding:14px 16px;
    border-radius:14px;
    border:1.5px solid rgba(122,33,49,.25);
    background:#FFFCF6;
    color:var(--ink);
    outline:none;
    transition:border-color .3s;
  }
  .field input:focus{ border-color:var(--saffron); }

  .personal-card{
    display:none;
    width:100%;
    background:linear-gradient(160deg, var(--maroon), var(--maroon-deep));
    color:var(--cream);
    border-radius:22px;
    padding:clamp(26px,6vw,38px) clamp(22px,6vw,30px);
    text-align:left;
    box-shadow:0 26px 55px -20px rgba(78,20,32,.55);
    opacity:0; transform:translateY(10px) scale(.98);
    transition:opacity .7s var(--ease), transform .7s var(--ease);
  }
  .personal-card.show{ display:block; opacity:1; transform:translateY(0) scale(1); }
  .personal-card .tag{ color:var(--gold-light); font-size:.82rem; letter-spacing:.02em; }
  .personal-card h3{ font-size:clamp(1.4rem,4.8vw,1.8rem); margin:8px 0 14px; color:var(--cream); }
  .personal-card p{ font-family:'Fraunces',serif; font-size:1.1rem; line-height:1.55; margin:0 0 14px; }
  .personal-card .sig{ font-style:italic; color:var(--gold-light); font-size:.95rem; }

  .toast{
    position:fixed;
    bottom:28px;
    left:50%;
    transform:translateX(-50%) translateY(20px);
    background:var(--ink);
    color:var(--cream);
    padding:12px 22px;
    border-radius:999px;
    font-size:.88rem;
    opacity:0;
    pointer-events:none;
    transition:opacity .4s var(--ease), transform .4s var(--ease);
    z-index:20;
  }
  .toast.show{ opacity:1; transform:translateX(-50%) translateY(0); }

  /* ---------- Sound toggle ---------- */
  .sound-toggle{
    position:absolute;
    top:max(18px, env(safe-area-inset-top));
    right:18px;
    z-index:10;
    background:rgba(255,255,255,.12);
    border:1px solid rgba(255,255,255,.25);
    border-radius:999px;
    width:38px;
    height:38px;
    display:flex;
    align-items:center;
    justify-content:center;
    cursor:pointer;
    color:var(--cream);
    font-size:16px;
    backdrop-filter:blur(6px);
  }
  #screen2 .sound-toggle, #screen3 .sound-toggle, #screen4 .sound-toggle{
    background:rgba(122,33,49,.08);
    border:1px solid rgba(122,33,49,.2);
    color:var(--maroon);
  }

  .step-dots{ display:flex; gap:7px; }
  .step-dots span{ width:6px; height:6px; border-radius:50%; background:rgba(122,33,49,.2); }
  .step-dots span.on{ background:var(--saffron); }
  #screen1 .step-dots span{ background:rgba(248,238,220,.25); }
  #screen1 .step-dots span.on{ background:var(--gold-light); }

  /* ---------- Desktop enhancements ---------- */
  .garland{ position:absolute; top:0; width:120px; opacity:.5; pointer-events:none; display:none; }
  .garland.left{ left:28px; }
  .garland.right{ right:28px; transform:scaleX(-1); }

  @media (min-width:900px){
    .screen-inner{ max-width:560px; gap:30px; }
    .garland{ display:block; }
    .headline-1{ font-size:2.8rem; }
    .headline-2{ font-size:3.1rem; }
  }

  @media (max-height:640px){
    .glyph{ width:110px; }
    .ganpati-reveal{ width:120px; }
  }

  /* ============================================================ */
  /* ---------- PREMIUM DEVELOPER FOOTER (ONE LINE) ---------- */
  /* ============================================================ */
  .dev-footer{
    position:fixed;
    left:50%;
    bottom:max(12px, env(safe-area-inset-bottom));
    transform:translateX(-50%);
    z-index:60;
    display:inline-flex;
    align-items:center;
    justify-content:center;
    flex-wrap:nowrap;
    white-space:nowrap;
    gap:8px;
    padding:5px 16px;
    border-radius:999px;
    background:linear-gradient(135deg, rgba(24,17,9,.78), rgba(15,10,5,.78));
    border:1px solid rgba(199,154,63,.4);
    backdrop-filter:blur(10px);
    -webkit-backdrop-filter:blur(10px);
    box-shadow:
      0 6px 18px -8px rgba(0,0,0,.6),
      inset 0 0 10px rgba(199,154,63,.12);
    pointer-events:none;
    user-select:none;
    animation:footerGlow 4s ease-in-out infinite;
    max-width:92vw;
  }

  @keyframes footerGlow{
    0%,100%{
      box-shadow:
        0 6px 18px -8px rgba(0,0,0,.6),
        inset 0 0 10px rgba(199,154,63,.12);
    }
    50%{
      box-shadow:
        0 8px 22px -8px rgba(199,154,63,.4),
        inset 0 0 14px rgba(199,154,63,.24);
    }
  }

  .dev-footer .line{
    width:14px;
    height:1px;
    flex:0 0 auto;
    background:linear-gradient(90deg, transparent, var(--gold), transparent);
  }

  .dev-footer .dev-text{
    font-family:'Fraunces',serif;
    font-size:.6rem;
    letter-spacing:.14em;
    font-weight:600;
    text-transform:uppercase;
    white-space:nowrap;
    background:linear-gradient(90deg,
      #AD7C2A 0%,
      #E7CE8F 25%,
      #FFF7DF 50%,
      #E7CE8F 75%,
      #AD7C2A 100%);
    background-size:200% auto;
    -webkit-background-clip:text;
    background-clip:text;
    -webkit-text-fill-color:transparent;
    color:transparent;
    animation:shineSweep 5s linear infinite;
  }

  @keyframes shineSweep{
    0%{ background-position:200% center; }
    100%{ background-position:-200% center; }
  }

  .dev-footer .dev-name{
    font-family:'Fraunces',serif;
    font-style:italic;
    font-weight:600;
    color:#F8EEDC;
    font-size:.68rem;
    letter-spacing:.1em;
    text-transform:uppercase;
    white-space:nowrap;
    text-shadow:0 0 10px rgba(231,206,143,.55);
  }

  @media (max-width:380px){
    .dev-footer{ padding:4px 12px; gap:6px; }
    .dev-footer .dev-text{ font-size:.52rem; letter-spacing:.1em; }
    .dev-footer .dev-name{ font-size:.6rem; letter-spacing:.06em; }
    .dev-footer .line{ width:10px; }
  }
</style>
</head>
<body>

<div id="stage">

  <!-- ================= SCREEN 1 : ARRIVAL ================= -->
  <section class="screen active" id="screen1">
    <div class="bg-mandala"></div>
    <svg class="arch-outline" viewBox="0 0 200 260" fill="none"><path d="M10 260V90C10 40 50 10 100 10C150 10 190 40 190 90V260" stroke="#E7CE8F" stroke-width="1.5"/><path d="M30 260V95C30 55 60 30 100 30C140 30 170 55 170 95V260" stroke="#E7CE8F" stroke-width="1"/></svg>
    <div class="particle-field" id="field1"></div>
    <div class="sound-toggle" id="soundToggle">🔔</div>

    <div class="screen-inner">
      <span class="eyebrow-line" id="eyebrow1">Ganesh Chaturthi</span>

      <div class="doorway" id="doorway">
        <div class="murti-spot">
          <svg class="glyph" viewBox="0 0 220 240" fill="none" xmlns="http://www.w3.org/2000/svg">
            <defs>
              <linearGradient id="skinA" x1="60" y1="30" x2="160" y2="180"><stop offset="0%" stop-color="#E7A672"/><stop offset="100%" stop-color="#B5693A"/></linearGradient>
              <linearGradient id="earA" x1="0" y1="60" x2="80" y2="160"><stop offset="0%" stop-color="#D98B5F"/><stop offset="100%" stop-color="#9C5730"/></linearGradient>
              <linearGradient id="clothA" x1="40" y1="170" x2="180" y2="240"><stop offset="0%" stop-color="#C0392B"/><stop offset="100%" stop-color="#6E1B26"/></linearGradient>
              <linearGradient id="crownA" x1="70" y1="10" x2="150" y2="55"><stop offset="0%" stop-color="#F3D488"/><stop offset="100%" stop-color="#AD7C2A"/></linearGradient>
            </defs>
            <path d="M38 238C38 192 58 170 110 170C162 170 182 192 182 238Z" fill="url(#clothA)"/>
            <path d="M55 178C75 168 145 168 165 178" stroke="#E7CE8F" stroke-width="2" opacity=".6"/>
            <path d="M96 163L124 163L119 178L101 178Z" fill="url(#skinA)"/>
            <path d="M38 72C8 84 2 132 32 152C50 164 68 146 66 118C64 96 58 80 38 72Z" fill="url(#earA)"/>
            <path d="M40 84C24 94 20 126 38 140C48 148 58 138 56 120C54 104 50 92 40 84Z" fill="#E7A672" opacity=".55"/>
            <path d="M182 72C212 84 218 132 188 152C170 164 152 146 154 118C156 96 162 80 182 72Z" fill="url(#earA)"/>
            <path d="M180 84C196 94 200 126 182 140C172 148 162 138 164 120C166 104 170 92 180 84Z" fill="#E7A672" opacity=".55"/>
            <circle cx="35" cy="112" r="4.5" fill="#F3D488"/>
            <circle cx="185" cy="112" r="4.5" fill="#F3D488"/>
            <path d="M64 58C64 26 156 26 156 58C161 92 160 132 138 154C122 170 98 170 82 154C60 132 59 92 64 58Z" fill="url(#skinA)"/>
            <path d="M108 92C102 118 96 140 102 162C105 176 120 184 133 176C141 171 139 159 130 157C123 155 121 148 125 140" stroke="#8A4E29" stroke-width="15" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
            <path d="M132 152C139 148 145 152 141 159C138 164 131 162 132 152Z" fill="#F5EDE0"/>
            <path d="M70 44L110 12L150 44Z" fill="url(#crownA)"/>
            <rect x="66" y="40" width="88" height="13" rx="5" fill="url(#crownA)"/>
            <circle cx="110" cy="14" r="6" fill="#F3D488"/>
            <circle cx="92" cy="46" r="3.4" fill="#C0392B"/>
            <circle cx="128" cy="46" r="3.4" fill="#3E7A5C"/>
            <circle cx="110" cy="68" r="3" fill="#7A2131"/>
            <path d="M86 76C92 72 100 72 105 76" stroke="#6E1B26" stroke-width="2.4" stroke-linecap="round" fill="none"/>
            <path d="M115 76C120 72 128 72 134 76" stroke="#6E1B26" stroke-width="2.4" stroke-linecap="round" fill="none"/>
            <path d="M86 88C92 94 100 94 105 88" stroke="#3A2213" stroke-width="3" stroke-linecap="round" fill="none"/>
            <path d="M115 88C120 94 128 94 134 88" stroke="#3A2213" stroke-width="3" stroke-linecap="round" fill="none"/>
            <circle cx="93" cy="89" r="1.6" fill="#F8EEDC"/>
            <circle cx="122" cy="89" r="1.6" fill="#F8EEDC"/>
          </svg>
        </div>
        <div class="door-panel left"></div>

        <div class="door-panel right"></div>
        <div class="door-flash" id="doorFlash"></div>
      </div>
      <span class="door-hint" id="doorHint">Tap the doors to open them</span>

      <h1 class="headline-1" id="headline1">Bappa is at the temple doors</h1>
      <p class="sub-1" id="sub1">Before the celebration begins open them and welcome Him in.</p>

      <button class="btn btn-primary" id="btnWelcome">Open the doors</button>
      <div class="step-dots"><span class="on"></span><span></span><span></span><span></span></div>
    </div>
  </section>

  <!-- ================= SCREEN 2 : TRANSFORMATION ================= -->
  <section class="screen" id="screen2">
    <div class="bg-mandala"></div>
    <svg class="arch-outline" viewBox="0 0 200 260" fill="none" style="opacity:.14"><path d="M10 260V90C10 40 50 10 100 10C150 10 190 40 190 90V260" stroke="#7A2131" stroke-width="1.5"/><path d="M30 260V95C30 55 60 30 100 30C140 30 170 55 170 95V260" stroke="#7A2131" stroke-width="1"/></svg>
    <div class="particle-field" id="field2"></div>
    <div class="petal-field" id="petals2"></div>
    <div class="sound-toggle">&#x1F514 </div>
    <svg class="garland left" viewBox="0 0 100 200"><g fill="#DE7B34"><circle cx="20" cy="20" r="7"/><circle cx="35" cy="45" r="7"/><circle cx="18" cy="70" r="7"/><circle cx="34" cy="95" r="7"/><circle cx="20" cy="120" r="7"/></g></svg>
    <svg class="garland right" viewBox="0 0 100 200"><g fill="#DE7B34"><circle cx="20" cy="20" r="7"/><circle cx="35" cy="45" r="7"/><circle cx="18" cy="70" r="7"/><circle cx="34" cy="95" r="7"/><circle cx="20" cy="120" r="7"/></g></svg>

    <div class="screen-inner">
      <div class="diya-row" id="diyaRow">
        <!-- 5 diyas -->
        <svg class="diya" viewBox="0 0 26 34"><ellipse class="flame" cx="13" cy="9" rx="4" ry="8" fill="#DE7B34"/><path d="M2 20C2 20 6 26 13 26C20 26 24 20 24 20C24 27 19 32 13 32C7 32 2 27 2 20Z" fill="#7A2131"/></svg>
        <svg class="diya" viewBox="0 0 26 34"><ellipse class="flame" cx="13" cy="9" rx="4" ry="8" fill="#DE7B34"/><path d="M2 20C2 20 6 26 13 26C20 26 24 20 24 20C24 27 19 32 13 32C7 32 2 27 2 20Z" fill="#7A2131"/></svg>
        <svg class="diya" viewBox="0 0 26 34"><ellipse class="flame" cx="13" cy="9" rx="4" ry="8" fill="#DE7B34"/><path d="M2 20C2 20 6 26 13 26C20 26 24 20 24 20C24 27 19 32 13 32C7 32 2 27 2 20Z" fill="#7A2131"/></svg>
        <svg class="diya" viewBox="0 0 26 34"><ellipse class="flame" cx="13" cy="9" rx="4" ry="8" fill="#DE7B34"/><path d="M2 20C2 20 6 26 13 26C20 20 24 20 24 20C24 27 19 32 13 32C7 32 2 27 2 20Z" fill="#7A2131"/></svg>
        <svg class="diya" viewBox="0 0 26 34"><ellipse class="flame" cx="13" cy="9" rx="4" ry="8" fill="#DE7B34"/><path d="M2 20C2 20 6 26 13 26C20 20 24 20 24 20C24 27 19 32 13 32C7 32 2 27 2 20Z" fill="#7A2131"/></svg>
      </div>

      <svg class="ganpati-reveal" viewBox="0 0 220 240" fill="none" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <linearGradient id="skinB" x1="60" y1="30" x2="160" y2="180"><stop offset="0%" stop-color="#E7A672"/><stop offset="100%" stop-color="#B5693A"/></linearGradient>
          <linearGradient id="earB" x1="0" y1="60" x2="80" y2="160"><stop offset="0%" stop-color="#D98B5F"/><stop offset="100%" stop-color="#9C5730"/></linearGradient>
          <linearGradient id="clothB" x1="40" y1="170" x2="180" y2="240"><stop offset="0%" stop-color="#C0392B"/><stop offset="100%" stop-color="#6E1B26"/></linearGradient>
          <linearGradient id="crownB" x1="70" y1="10" x2="150" y2="55"><stop offset="0%" stop-color="#F3D488"/><stop offset="100%" stop-color="#AD7C2A"/></linearGradient>
        </defs>
        <path d="M38 238C38 192 58 170 110 170C162 170 182 192 182 238Z" fill="url(#clothB)"/>
        <path d="M55 178C75 168 145 168 165 178" stroke="#E7CE8F" stroke-width="2" opacity=".6"/>
        <path d="M96 163L124 163L119 178L101 178Z" fill="url(#skinB)"/>
        <path d="M38 72C8 84 2 132 32 152C50 164 68 146 66 118C64 96 58 80 38 72Z" fill="url(#earB)"/>
        <path d="M40 84C24 94 20 126 38 140C48 148 58 138 56 120C54 104 50 92 40 84Z" fill="#E7A672" opacity=".55"/>
        <path d="M182 72C212 84 218 132 188 152C170 164 152 146 154 118C156 96 162 80 182 72Z" fill="url(#earB)"/>
        <path d="M180 84C196 94 200 126 182 140C172 148 162 138 164 120C166 104 170 92 180 84Z" fill="#E7A672" opacity=".55"/>
        <circle cx="35" cy="112" r="4.5" fill="#F3D488"/>
        <circle cx="185" cy="112" r="4.5" fill="#F3D488"/>
        <path d="M64 58C64 26 156 26 156 58C161 92 160 132 138 154C122 170 98 170 82 154C60 132 59 92 64 58Z" fill="url(#skinB)"/>
        <path d="M108 92C102 118 96 140 102 162C105 176 120 184 133 176C141 171 139 159 130 157C123 155 121 148 125 140" stroke="#8A4E29" stroke-width="15" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
        <path d="M132 152C139 148 145 152 141 159C138 164 131 162 132 152Z" fill="#F5EDE0"/>
        <path d="M70 44L110 12L150 44Z" fill="url(#crownB)"/>
        <rect x="66" y="40" width="88" height="13" rx="5" fill="url(#crownB)"/>
        <circle cx="110" cy="14" r="6" fill="#F3D488"/>
        <circle cx="92" cy="46" r="3.4" fill="#C0392B"/>
        <circle cx="128" cy="46" r="3.4" fill="#3E7A5C"/>
        <circle cx="110" cy="68" r="3" fill="#7A2131"/>
        <path d="M86 76C92 72 100 72 105 76" stroke="#6E1B26" stroke-width="2.4" stroke-linecap="round" fill="none"/>
        <path d="M115 76C120 72 128 72 134 76" stroke="#6E1B26" stroke-width="2.4" stroke-linecap="round" fill="none"/>
        <path d="M86 88C92 94 100 94 105 88" stroke="#3A2213" stroke-width="3" stroke-linecap="round" fill="none"/>
        <path d="M115 88C120 94 128 94 134 88" stroke="#3A2213" stroke-width="3" stroke-linecap="round" fill="none"/>
        <circle cx="93" cy="89" r="1.6" fill="#F8EEDC"/>
        <circle cx="122" cy="89" r="1.6" fill="#F8EEDC"/>
      </svg>

      <h2 class="headline-2">Ganpati Bappa Morya</h2>
      <p class="sub-2">He has arrived and He's brought something for you</p>

      <button class="btn btn-primary cta-2" id="btnToBlessing">See what it is</button>
      <div class="step-dots"><span></span><span class="on"></span><span></span><span></span></div>
    </div>
  </section>

  <!-- ================= SCREEN 3 : BLESSING ================= -->
  <section class="screen" id="screen3">
    <div class="bg-mandala"></div>
    <div class="particle-field" id="field3"></div>
    <div class="sound-toggle">&#x1F514 </div>
    <svg class="garland left" viewBox="0 0 100 200"><g fill="#C79A3F"><circle cx="20" cy="20" r="7"/><circle cx="35" cy="45" r="7"/><circle cx="18" cy="70" r="7"/><circle cx="34" cy="95" r="7"/><circle cx="20" cy="120" r="7"/></g></svg>
    <svg class="garland right" viewBox="0 0 100 200"><g fill="#C79A3F"><circle cx="20" cy="20" r="7"/><circle cx="35" cy="45" r="7"/><circle cx="18" cy="70" r="7"/><circle cx="34" cy="95" r="7"/><circle cx="20" cy="120" r="7"/></g></svg>

    <div class="screen-inner">
      <span class="kicker">A small blessing</span>
      <h2 class="headline-3">Something for you to carry today</h2>

      <div class="blessing-card">
        <p id="blessingText">Tap below to receive one</p>
      </div>

      <div class="row-actions">
        <button class="btn btn-primary" id="btnReveal">Receive your blessing</button>
        <button class="btn-text" id="btnAnother" style="display:none;">Give me another →</button>
      </div>

      <button class="btn btn-ghost" id="btnToPersonal" style="display:none;">Send this to someone</button>
      <div class="step-dots"><span></span><span></span><span class="on"></span><span></span></div>
    </div>
  </section>

  <!-- ================= SCREEN 4 : PERSONALIZE ================= -->
  <section class="screen" id="screen4">
    <div class="bg-mandala"></div>
    <div class="particle-field" id="field4"></div>
    <div class="sound-toggle">&#x1F514 </div>

    <div class="screen-inner">
      <span class="kicker">Pass it on</span>
      <h2 class="headline-3">Who should receive Bappa's blessing?</h2>

      <div class="field">
        <label for="nameInput">Their name</label>
        <input type="text" id="nameInput" placeholder="e.g. Shivani" maxlength="30">
      </div>

      <button class="btn btn-primary" id="btnGenerate">Create their blessing</button>

      <div class="personal-card" id="personalCard">
        <span class="tag">Bappa's blessing for</span>
        <h3 id="personalName">—</h3>
        <p id="personalText">—</p>
        <span class="sig">Ganpati Bappa Morya </span>
      </div>

      <button class="btn btn-ghost" id="btnShare" style="display:none;">Share this blessing</button>
      <button class="btn-text" id="btnRestart">Start over</button>
      <div class="step-dots"><span></span><span></span><span></span><span class="on"></span></div>
    </div>
  </section>

</div>

<div class="toast" id="toast">Copied to clipboard</div>

<!-- ================= PREMIUM DEVELOPER FOOTER (ONE LINE) ================= -->
<div class="dev-footer" aria-hidden="true">
  <span class="line"></span>
  <span class="dev-text">DEVELOPER :-</span>
  <span class="dev-name">MR AMAR</span>
  <span class="line"></span>
</div>

<script>
(function(){
  "use strict";

  /* ---------------- Screen navigation ---------------- */
  const screens = ["screen1","screen2","screen3","screen4"];
  function goTo(id){
    screens.forEach(s => document.getElementById(s).classList.toggle("active", s === id));
  }

  /* ---------------- Sound (tiny synthesized bell) ---------------- */
  let audioCtx = null;
  let soundOn = true;
  function ensureAudio(){
    if (!audioCtx){
      try{ audioCtx = new (window.AudioContext || window.webkitAudioContext)(); }catch(e){ audioCtx = null; }
    }
  }
  function chime(){
    if (!soundOn) return;
    ensureAudio();
    if (!audioCtx) return;
    const now = audioCtx.currentTime;
    [880, 1320].forEach((freq, i) => {
      const osc = audioCtx.createOscillator();
      const gain = audioCtx.createGain();
      osc.type = "sine";
      osc.frequency.value = freq;
      gain.gain.setValueAtTime(0.0001, now);
      gain.gain.exponentialRampToValueAtTime(i === 0 ? 0.12 : 0.05, now + 0.02);
      gain.gain.exponentialRampToValueAtTime(0.0001, now + 1.1);
      osc.connect(gain).connect(audioCtx.destination);
      osc.start(now);
      osc.stop(now + 1.15);
    });
  }
  document.querySelectorAll(".sound-toggle").forEach(btn => {
    btn.addEventListener("click", () => {
      soundOn = !soundOn;
      document.querySelectorAll(".sound-toggle").forEach(b => b.textContent = soundOn ? "🔔" : "🔕");
      if (soundOn) ensureAudio();
    });
  });

  /* ---------------- Ambient particles ---------------- */
  function seedDust(containerId, count){
    const el = document.getElementById(containerId);
    if (!el) return;
    for (let i = 0; i < count; i++){
      const d = document.createElement("div");
      d.className = "dust";
      const size = 3 + Math.random() * 5;
      d.style.width = size + "px";
      d.style.height = size + "px";
      d.style.left = Math.random() * 100 + "%";
      d.style.top = Math.random() * 100 + "%";
      d.style.animationDelay = (Math.random() * 3.6) + "s";
      d.style.animationDuration = (2.8 + Math.random() * 2.4) + "s";
      el.appendChild(d);
    }
  }
  seedDust("field1", 22);
  seedDust("field2", 18);
  seedDust("field3", 14);
  seedDust("field4", 14);

  function seedPetals(containerId, count){
    const el = document.getElementById(containerId);
    if (!el) return;
    for (let i = 0; i < count; i++){
      const p = document.createElement("div");
      p.className = "petal";
      p.style.left = Math.random() * 100 + "%";
      p.style.animationDuration = (5 + Math.random() * 4) + "s";
      p.style.animationDelay = (Math.random() * 5) + "s";
      p.style.opacity = 0.55 + Math.random() * 0.35;
      const scale = 0.7 + Math.random() * 0.8;
      p.style.transform = "scale(" + scale + ")";
      el.appendChild(p);
    }
  }
  seedPetals("petals2", 16);

  /* ---------------- Screen 1 : door opens, murti revealed, then -> 2 ---------------- */
  let doorIsOpen = false;
  const doorway = document.getElementById("doorway");
  const doorFlash = document.getElementById("doorFlash");
  const doorHint = document.getElementById("doorHint");
  const eyebrow1 = document.getElementById("eyebrow1");
  const headline1 = document.getElementById("headline1");
  const sub1 = document.getElementById("sub1");
  const btnWelcome = document.getElementById("btnWelcome");

  function openTempleDoors(){
    doorIsOpen = true;
    chime();
    doorway.classList.add("open");
    doorFlash.classList.add("burst");
    seedDust("field1", 14);
    doorHint.style.opacity = "0";
    setTimeout(() => {
      eyebrow1.textContent = "He's home";
      headline1.textContent = "Bappa has arrived.";
      sub1.textContent = "Take Him in for a moment before the celebration begins.";
      btnWelcome.textContent = "Begin the celebration";
    }, 750);
  }

  function beginCelebration(){
    chime();
    goTo("screen2");
    const s2 = document.getElementById("screen2");
    const diyaRow = document.getElementById("diyaRow");
    requestAnimationFrame(() => {
      diyaRow.classList.add("lit");
      setTimeout(() => { s2.classList.add("revealed"); }, 650);
    });
  }

  btnWelcome.addEventListener("click", () => {
    if (!doorIsOpen){ openTempleDoors(); return; }
    beginCelebration();
  });
  doorway.addEventListener("click", () => {
    if (!doorIsOpen) openTempleDoors();
  });

  /* ---------------- Screen 2 -> 3 ---------------- */
  document.getElementById("btnToBlessing").addEventListener("click", () => {
    chime();
    goTo("screen3");
  });

  /* ---------------- Screen 3 : blessings ---------------- */
  const blessings = [
    "May every obstacle you meet become a quiet teacher, and every ending open onto something better.",
    "May your home stay filled with laughter, your table with abundance, and your heart with ease.",
    "May the courage of new beginnings walk with you long after today.",
    "May what troubles you loosen its grip, one small mercy at a time.",
    "May you be surrounded by people who choose you, and days that feel like your own.",
    "May your effort find its reward, and your patience find its season.",
    "May Bappa clear the road ahead, so you only have to walk it.",
    "May you carry this year lightly — less to fear, more to hope for.",
    "May the noise settle, and what matters most become clear.",
    "May joy find you even on the days you forget to look for it."
  ];
  let lastIndex = -1;
  let currentBlessing = "";
  function pickBlessing(){
    let idx;
    do { idx = Math.floor(Math.random() * blessings.length); } while (idx === lastIndex && blessings.length > 1);
    lastIndex = idx;
    currentBlessing = blessings[idx];
    return currentBlessing;
  }

  const blessingText = document.getElementById("blessingText");
  const btnReveal = document.getElementById("btnReveal");
  const btnAnother = document.getElementById("btnAnother");
  const btnToPersonal = document.getElementById("btnToPersonal");

  function showBlessing(){
    chime();
    blessingText.classList.remove("show");
    setTimeout(() => {
      blessingText.textContent = pickBlessing();
      blessingText.classList.add("show");
    }, 220);
  }

  btnReveal.addEventListener("click", () => {
    showBlessing();
    btnReveal.style.display = "none";
    btnAnother.style.display = "inline-block";
    btnToPersonal.style.display = "inline-block";
  });
  btnAnother.addEventListener("click", showBlessing);

  btnToPersonal.addEventListener("click", () => {
    goTo("screen4");
  });

  /* ---------------- Screen 4 : personalize ---------------- */
  const nameInput = document.getElementById("nameInput");
  const personalCard = document.getElementById("personalCard");
  const personalName = document.getElementById("personalName");
  const personalText = document.getElementById("personalText");
  const btnShare = document.getElementById("btnShare");

  document.getElementById("btnGenerate").addEventListener("click", () => {
    const name = (nameInput.value || "").trim();
    if (!name){
      nameInput.focus();
      nameInput.style.borderColor = "#7A2131";
      setTimeout(() => { nameInput.style.borderColor = ""; }, 900);
      return;
    }
    chime();
    personalName.textContent = name;
    personalText.textContent = currentBlessing || pickBlessing();
    personalCard.classList.add("show");
    btnShare.style.display = "inline-block";
  });

  btnShare.addEventListener("click", async () => {
    const name = personalName.textContent;
    const text = "Bappa's blessing for " + name + " —\n\n" + personalText.textContent + "\n\nGanpati Bappa Morya 🐘";
    if (navigator.share){
      try{ await navigator.share({ text: text, title: "A blessing from Bappa" }); return; }
      catch(e){ /* user cancelled or unsupported, fall through to copy */ }
    }
    try{
      await navigator.clipboard.writeText(text);
      showToast("Copied — ready to share");
    }catch(e){
      showToast("Couldn't copy — select and copy manually");
    }
  });

  document.getElementById("btnRestart").addEventListener("click", () => {
    nameInput.value = "";
    personalCard.classList.remove("show");
    btnShare.style.display = "none";
    blessingText.classList.remove("show");
    blessingText.textContent = "Tap below to receive one.";
    btnReveal.style.display = "inline-block";
    btnAnother.style.display = "none";
    btnToPersonal.style.display = "none";
    document.getElementById("screen2").classList.remove("revealed");
    document.getElementById("diyaRow").classList.remove("lit");
    doorIsOpen = false;
    doorway.classList.remove("open");
    doorFlash.classList.remove("burst");
    doorHint.style.opacity = "1";
    eyebrow1.textContent = "Ganesh Chaturthi";
    headline1.textContent = "Bappa is at the temple doors.";
    sub1.textContent = "Before the celebration begins, open them and welcome Him in.";
    btnWelcome.textContent = "Open the doors";
    goTo("screen1");
  });

  /* ---------------- Toast ---------------- */
  let toastTimer = null;
  function showToast(msg){
    const t = document.getElementById("toast");
    t.textContent = msg;
    t.classList.add("show");
    clearTimeout(toastTimer);
    toastTimer = setTimeout(() => t.classList.remove("show"), 2400);
  }
})();
</script>
</body>
</html>